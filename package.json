{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "heroku-postbuild": "lerna run migrations --scope salty-caramel-backend"
  },
  "devDependencies": {
    "lerna": "^6.3.0"
  }
}
